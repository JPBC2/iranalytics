// You can also use the gap package https://pub.dev/packages/gap

/* import 'package:flutter/material.dart';
import 'package:iranalytics/app/res/responsive.res.dart';

class ResponsiveGap extends StatelessWidget {
  const ResponsiveGap({
    Key? key,
    required this.gap,
  }) : super(key: key);

  final double gap;

  @override
  Widget build(BuildContext context) {
    return MediaQuery.of(context).size.width > ScreenSizes.md
        ? SizedBox(width: gap)
        : SizedBox(height: gap);
  }
} */
